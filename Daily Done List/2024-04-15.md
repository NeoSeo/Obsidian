# Python
크롤링 이어서 공부:
``` python
## 기본적 문구
import requests
from bs4 import BeautifulSoup ## s 반드시 대문자
res = requests.get('url')
soup = BeautifulSoup(res.content, 'html.parser')

## 여기서부터는 원하는 데이터 추출
items = soup.select('ur a')
for i in items:
	print(i.get_text().split('(').) ## split




```